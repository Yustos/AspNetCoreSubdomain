@model string
@{
    ViewData["Title"] = "Home Page";
}

@Html.Partial("_NavPills")
@Html.Partial("_DisplayModel", Model)
<div class="container-fluid">
    <div class="row">
        User: 
        @Html.Raw(User.Identity.Name)
    </div>
    <div class="row">
        <div class="col-xs-12">
            @Html.Partial("Urls")
            
        </div>
    </div>
</div>
